import { products, promotedProducts, allProducts } from "./products.js";

export default {
	// todo: make this actually affect the page
	storeInfo: {
		name: "Sklep",
		image: "include/image/icon.png"
	},
	products,
	promotedProducts,
	allProducts
}
